#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        float a,b,c;
        char d[20];
        scanf("%c %c %f",&d,&e,&)
        if(a>b&&b>c){
            printf("Standard: %f, Premium: %f, VIP: %f",)
        }
        printf(" ")
    }
}